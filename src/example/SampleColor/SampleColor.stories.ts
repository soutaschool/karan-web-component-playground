import type { Meta, StoryObj } from "@storybook/web-components";
import "./SampleColor";

const meta: Meta = {
  title: "Example/SampleColor",
  component: "sample-color",
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj;

export const Default: Story = {};
